import { DashboardPage } from '@/components/Dashboard'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <DashboardPage />
    </div>
}

export default page