import { SettingsPage } from '@/components/Settings'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <SettingsPage />
    </div>
}

export default page