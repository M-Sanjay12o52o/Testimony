import Landing from '@/components/Landing'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <Landing />
    </div>
}

export default page