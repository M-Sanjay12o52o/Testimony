import ContactPage from '@/components/Contact'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <ContactPage />
    </div>
}

export default page