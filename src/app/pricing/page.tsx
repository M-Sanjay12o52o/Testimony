import { PricingPage } from '@/components/Pricing'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <PricingPage />
    </div>
}

export default page